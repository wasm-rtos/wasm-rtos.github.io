name: Build browser runtime

permissions:
  contents: write

on:
  workflow_dispatch:
  push:
    paths:
      - "os/main.c"
      - "os/wasm-rtos"
      - ".gitmodules"
      - "build-browser.sh"
      - ".github/workflows/build-browser.yml"
  pull_request:
    paths:
      - "os/main.c"
      - "os/wasm-rtos"
      - ".gitmodules"
      - "build-browser.sh"
      - ".